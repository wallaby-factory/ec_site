'use client'

// User admin components
// Note: User model does not have a role field - role management is for AdminUser only

export function UserActions({ userId }: { userId: string }) {
    return (
        <div className="text-xs text-slate-500">
            {/* User actions can be added here if needed */}
        </div>
    )
}
